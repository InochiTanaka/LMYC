@model LmycDataLib.ApplicationUser

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<div>
    <hr />
    @{
        string tempName = Model.UserName;
        string tempEmail = Model.Email;

        if (tempName == "a" && tempEmail == "a@a.a")
        {

            string Message = "You cannot delete UserName \" a \" .";
            <dl class="dl-horizontal">
                @Html.DisplayFor(model => Message)
            </dl>
            <dl class="dl-horizontal">
                @Html.ActionLink("Back to List", "Index")
            </dl>
        }
        else
        {
            <h3>Are you sure you want to delete this?</h3>
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.UserName)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.UserName)
                </dd>

                <dt>
                    @Html.DisplayNameFor(model => model.Roles)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Roles)
                </dd>

                <dt>
                    @Html.DisplayNameFor(model => model.Email)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Email)
                </dd>
            </dl>
            using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
                <div class="form-actions no-color">
                    <input type="submit" value="Delete" class="btn btn-default" />
                    @Html.ActionLink("Back to List", "Index")
                </div>
            }
        }
    }

</div>



